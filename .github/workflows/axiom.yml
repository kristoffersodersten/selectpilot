name: Axiom Enforcement

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  axiom:
    uses: kristoffersodersten/axiom-core/.github/workflows/axiom-downstream.yml@main
    with:
      intent_path: intent.json
      strict: true
