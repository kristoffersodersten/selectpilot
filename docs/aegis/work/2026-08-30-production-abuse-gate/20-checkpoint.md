# Checkpoint

## TodoCheckpointDraft

- Current todo: qualify the physical-smoke follow-up on Hetzner and GitHub, preserve the independent-review gate, and retain every unavailable production authority without laundering it into code completion.
- Completed: Linear global runtime laws, SelectPilot project truth, and GitHub PR state were re-read before mutation; the full-system gap/blind-spot/negative-space audit was refreshed; the signed intent remains valid. PR #22 source `93334b73e58bc122947eb6b8b866f3a2e65e0e50` passed all 9 exact-head GitHub checks in CI run `33812111260` and CodeQL run `33812111254`.
- Repository repairs: Store packaging now requires an explicit external entitlement public identity and exact clean SHA; ordinary CI proves the missing-identity failure path; mutable helper state is user-owned; local inference concurrency is bounded; exact model/context routing survives the agent pipeline and is visible in the extension. Physical inspection also found and repaired two installer/runtime gaps: transport `TimeoutError` now becomes explicit bridge HTTP 503, and both local and packaged LaunchAgents bind an aggregate hash over all active runtime code and policy. The local installer now includes the previously omitted `installation_manager.py` dependency.
- Remote evidence: Hetzner and exact-head GitHub passed lint, typecheck/build parity, 50 Node tests, 73 server tests, 7 Playwright journeys, privacy, billing, manifest/assets, negative Store packaging, frontier traceability, runtime-policy validation, stress contracts, and exact-SHA code admission. The admission report correctly remained `runtime_verified=false`, `physical_hardware_verified=false`, and `full_system_ok=false`.
- Physical M4 evidence: the explicit local prewarm completed in `130.599s` (`129.834s` model load) from external model storage. Once warm, two identical `action_brief` extractions completed in `3.722s` and `3.134s`, selected `gemma4:e2b-it-qat` at 16,384 context with `smallest_qualified_structured_model`, and produced identical canonical digest `8283d9866499bef776bc4d171b94d772944a04330ffed329774baa438c55b6d4`. An explicitly missing model returned degraded health and HTTP 503 without substitution; restoring the exact model recovered extraction in `3.446s`.
- External truth: PR #22 still requires one eligible independent approval; production DNS/TLS, Paddle credentials/lifecycle, authorized entitlement signer, Developer ID Installer/notarization, exact Store upload/publication, full browser first-use/export evidence, and remaining declared Apple hardware profiles remain outside repository authority.
- Next: obtain full remote regression and a new signed exact-head GitHub result for the physical-smoke repair, then execute protected merge and production gates only when their real authorities exist.

## ResumeStateHint

Resume from this checkpoint, re-read `10-intent.md`, verify `intent.json`, compare current head/worktree with this file, and refresh PR/Linear/external authority state before any completion claim.

## DriftCheckDraft

- Intent alignment: yes.
- Scope alignment: yes.
- New fallback or duplicate owner: no.
- Evidence sufficiency: repository hardening and one physical M4 structured-runtime slice are qualified; final follow-up GitHub admission, complete physical/browser runtime, protected review, and production activation evidence are still required.
- Decision: continue through remote and GitHub admission; fail closed on every full-system completion claim until its authority is present.
